// const mockErrorModel = require("../models/mock-error-model")
// const utilities = require("../utilities")

const mockErrorController = {}

/* ********
* Build mock error
* ********** */
mockErrorController.buildMockError = async function (req, res, next) {
    null.toString()
}

module.exports = mockErrorController